'use client'
import React, {FC} from 'react';
import {trpc} from "~/utils/trpcClient";

type PageProps = {}

const Page: FC<PageProps> = (props: PageProps) => {

    const {} = props




    return (
        <div>
            <span>access</span>

        </div>
    );
};

export default Page;